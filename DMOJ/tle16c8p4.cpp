#include <bits/stdc++.h>
using namespace std;

int X, D, P, memo[100010], N;

int main(){
    scanf("%d", &X);
    if(X == 1){
       scanf("%d %d %d", &D, &N, &P);

    }else{

    }

    return 0;
}