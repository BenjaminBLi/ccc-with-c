#include <bits/stdc++.h>
using namespace std;

int sz, P;

int main(){
    for(scanf("%d %d", &sz, &P); !(sz == 0 && P == 0); scanf("%d %d"), &sz, &P){
        int lsqrt = (int) sqrt(P);
        int hsqrt = lsqrt+1;
        else{

        }
    }
    return 0;
}
