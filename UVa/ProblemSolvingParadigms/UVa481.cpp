#include <bits/stdc++.h>
#define fori(i, n) for(int i = 0, _n = n; i < _n; i++)
using namespace std;

int tmp, len;
vector<int> lst;

void solve(){
    len = lst.size();
    vector<int> lis(len, 1);
    for(int i = 1;i <n; i++){
        //
    }
}

int main(){
    while(scanf("%d", &tmp) != EOF) lst.push_back(tmp);
    solve();
    return 0;
}
